name: test-modal
jobs_dir: jobs
n_attempts: 1
n_concurrent_trials: 1
timeout_multiplier: 1.0
instruction_path: /tmp/instruction.md
environment:
  type: modal
  preserve_env: never
  provider_config:
    timeout_secs: 600
agents:
  - name: oracle
datasets:
  - path: ./test-dataset
